Request the encrypted flag and use the decryption oracle's MAC error behavior to recover the keystream one byte at a time. The recovered keystream XORed with the ciphertext yields the flag plaintext.
